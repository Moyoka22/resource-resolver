# pytest.ini
[pytest]
minversion = 6.0
addopts = -ra -q
testpaths =
    tests
log_file_format = [ %(asctime)s - %(levelname)s - %(filename)s - %(name)s - %(funcName)s - %(lineno)d ]: %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_level = DEBUG
log_file = pytest.log